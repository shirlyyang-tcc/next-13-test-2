export const onRequestGet = async (context) => {
  // 请求腾讯云 mysql 进行读操作
  const { req } = context;
};
export const onRequestPost = async (context) => {
  // 请求腾讯云 mysql 进行写操作
  const { req } = context;
  
};
